@model IEnumerable<PUPFMIS.Models.RequestHeader>

@{
    ViewBag.Title = "Release Items";
    ViewBag.Description = "Home";
    Layout = "~/Views/Shared/_IndexLayout.cshtml";
}

@section BreadCrumb {
    <ol class="breadcrumb float-sm-right">
        <li class="breadcrumb-item"><a href="/"><i class="fa fa-dashboard"></i>@Html.ActionLink("PUP-FM Portal", "index", "Home")</a></li>
        <li class="breadcrumb-item">@Html.ActionLink("Release Items", "index")</li>
        <li class="breadcrumb-item">Home</li>
    </ol>
}

@section Header {
    <div class="col-md-12 text-right">
        <h3 class="card-title"><i class="fa fa-list-ul"></i> Approved Requests</h3>
    </div>
}

<table class="table table-bordered table-hover" id="example2">
    <thead>
        <tr>
            <th width="15%">
                @Html.DisplayNameFor(model => model.RISNo)
            </th>
            <th width="25%">
                @Html.DisplayNameFor(model => model.Office)
            </th>
            <th width="20%">
                @Html.DisplayNameFor(model => model.DateIssued)
            </th>
            <th width="20%">
                Issued by
            </th>
            <th width="20%">Action</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                RIS 2019-11-0012
            </td>
            <td>
                <b>Procurement Management Office</b>
                <br />
                <i>PMO Staff</i>
            </td>
            <td>
                @DateTime.Now.ToString("dd MMMM yyyy hh:mm tt")
            </td>
            <td>
                PSMO Issuance Staff
            </td>
            <td align="center">
                <button class="btn btn-success" onclick="window.location = '@Url.Action("RequestRelease", "Requests", new { type = "release", id = 0 })'"><i class="fa fa-share"></i> Release Request</button>
            </td>
        </tr>
        <tr>
            <td>
                RIS 2019-11-0045
            </td><td>
                <b>Office of the Vice President for Administration</b>
                <br />
                <i>OVPA Staff</i>
            </td>
            <td>
                @DateTime.Now.ToString("dd MMMM yyyy hh:mm tt")
            </td>
            <td>
                PSMO Issuance Staff
            </td>
            <td align="center">
                <button class="btn btn-success" onclick="window.location = '@Url.Action("RequestRelease", "Requests", new { type = "release", id = 0 })'"><i class="fa fa-share"></i> Release Request</button>
            </td>
        </tr>
        @*@foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Office)
                    </td>
                    <td>
                        Staff
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.RequestDate)
                    </td>
                    <td>

                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                    </td>
                </tr>
            }*@
    </tbody>
</table>
