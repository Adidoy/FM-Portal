<nav class="main-header navbar navbar-expand navbar-blue navbar-gray-dark">
    <ul class="navbar-nav">
        <li class="nav-item navbar-text">
            <span class="p-2" style="color:white; font-size:10pt;">
                <i class="far fa-user"></i> &nbsp; @( ((System.Security.Claims.ClaimsIdentity)User.Identity).FindFirst(d => d.Type == "Employee").Value ) &nbsp; | &nbsp; @( ((System.Security.Claims.ClaimsIdentity)User.Identity).FindFirst(d => d.Type == "Office").Value ) &nbsp; | &nbsp; @( ((System.Security.Claims.ClaimsIdentity)User.Identity).FindFirst(d => d.Type == "Designation").Value )
            </span>
        </li>

    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/" class="nav-link p-2" style="color:white; font-size:10pt;"><i class="fa fa-home"></i>&nbsp;Home</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="@Url.Action("Logout", "Users", new { Area = "" })" class="nav-link p-2" style="color:white; font-size:10pt;"><i class="fa fa-sign-out-alt"></i>&nbsp;Log Out</a>
        </li>
    </ul>
</nav>