@model IEnumerable<PUPFMIS.Models.APPHeader>

@{
    ViewBag.Title = "Procurement Activities";
    ViewBag.Description = "APP List";
    Layout = "~/Views/Shared/_IndexLayout.cshtml";
}

@section BreadCrumb {
    <ol class="breadcrumb float-sm-right">
        <li class="breadcrumb-item"><a href="/"><i class="fa fa-dashboard"></i>@Html.ActionLink("PUP-FM Portal", "index", "Home")</a></li>
        <li class="breadcrumb-item">@Html.ActionLink("Procurement Activities Home", "dashboard", "ProcurementPipelines", new { Area = "procurement" }, null)</li>
        <li class="breadcrumb-item">Home</li>
    </ol>
}

@section Header {
    <h3 class="card-title"><i class="fa fa-bookmark"></i> Annual Procument Plan List</h3>
}
<div class="row">
    <div class="col-sm-12">
        <div class="card-body table-responsive p-sm-0">
            <table class="table table-striped table-hover" id="example2">
                <thead>
                    <tr>
                        <th style="text-align:center; vertical-align:middle;">@Html.DisplayNameFor(d => d.FiscalYear)</th>
                        <th style="text-align:center; vertical-align:middle;">@Html.DisplayNameFor(d => d.ReferenceNo)</th>
                        <th style="text-align:center; vertical-align:middle;">APP Type</th>
                        <th style="text-align:center; vertical-align:middle;">Date Signed by HOPE</th>
                        <th style="text-align:center; vertical-align:middle;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td style="text-align:center; vertical-align:middle;">@Html.DisplayFor(d => item.FiscalYear)</td>
                            <td style="text-align:center; vertical-align:middle;">@Html.DisplayFor(d => item.ReferenceNo)</td>
                            <td style="text-align:center; vertical-align:middle;">@Html.DisplayFor(d => item.APPType)</td>
                            <td style="text-align:center; vertical-align:middle;">@( item.ApprovedAt == null ? "Pending" : ((DateTime)item.ApprovedAt).ToString("dd MMMM yyyy") )</td>
                            <td style="text-align:center; vertical-align:middle;">
                                <button class="btn btn-sm btn-success mb-1" style="width: 140px;" onclick="window.location ='@Url.Action("assignment-index", "ProcurementPipelines", new { Area = "procurement", ReferenceNo = item.ReferenceNo })'"><i class="fa fa-users"></i> Assign Projects</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

